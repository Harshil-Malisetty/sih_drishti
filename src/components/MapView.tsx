import type { ReactNode } from 'react';
export function BottomSheet({eyebrow,title,children,action,onAction}:{eyebrow?:string;title:string;children:ReactNode;action?:string;onAction?:()=>void}){return <section className="bottom-sheet" aria-live="polite"><i className="handle"/>{eyebrow&&<span className="sheet-eyebrow">{eyebrow}</span>}<h3>{title}</h3>{children}{action&&onAction&&<button className="primary" onClick={onAction}>{action}</button>}</section>}
