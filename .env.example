# Optional public, build-time URL reserved for the future FastAPI adapter.
# Leaving this empty keeps the existing browser demo. Setting it does NOT
# enable backend integration; the current services still use the demo store.
# Use an HTTPS base URL when an adapter is implemented. Never put secrets here.
VITE_API_BASE_URL=